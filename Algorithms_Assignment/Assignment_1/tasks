Task 1
1:sort set S using merge sort
2:substract x from elements of S(set) until it shows positive difference and store in SUM
3:find SUM in S(set) using binary search
for(i=0;s[i]!='\0';i++)
{
sub=x-s[i];
if(sub>0)
{
binary search for no sub
if(found)
return 1;
}
}

Task 2
1:eliminate all the element which is greater than x in the set S and copy the rest element of S in anothe arr[]
2:add first and last element of the arr[] and check whether it is equal to x 
3:if the sum of two no is eqal to x then we will terminate the programm by returning the pair

for(i=0;s[i]!='\0';i++){
if (s[i]<x){
count++
arr[count]=s[i]
}
for(i=0;arr[i]!='\0';i++){
sum=arr[i]+arr[count]
count--
if(sum==x)
break
}
